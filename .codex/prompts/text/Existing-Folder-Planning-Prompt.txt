This existing textbook notebook folder already has notebooks, but the work needs to be improved.

This turn is PLANNING ONLY.

Use the repo-local skills in D:\Geometry\.codex\skills. Explicitly open and follow:
- geometry-visualization-planner
- geometry-chapter-notebook-author
- geometry-notebook-qc

Also explicitly read the repo-local geometry library catalog:
- .codex\skills\geometry-visualization-planner\references\geometry-library-catalog.md
- any related catalog under the author or QC skill

Goal:
Plan a visualization-first improvement pass without throwing away good existing work.

Core failure mode to fix:
Some existing notebooks may have been created by scripts, may share generic repeated cells, may use weak/decorative visuals, or may ignore the installed geometry stack. The plan must identify those issues chapter by chapter and propose targeted improvements.

Planning-only rules:
- Do not modify files.
- Do not create, delete, or rewrite notebooks.
- Do not regenerate indexes.
- Do not edit scripts or utilities yet.
- Inspect before proposing changes.

Workflow:

1. Inspect folder structure:
   - AGENTS.md
   - 00-book-index.ipynb
   - chapter folders
   - scripts/
   - utils/
   - artifacts/
   - existing canonical notebooks

2. Inspect textbook PDF:
   - confirm title
   - confirm chapter page spans
   - confirm section hierarchy
   - compare with existing notebooks

3. Inspect current skills and library catalog:
   - identify whether notebooks use appropriate libraries
   - identify missed opportunities for geometry-specific visualization

4. Run or inspect existing audits if available:
   - notebook audit
   - visual audit
   - validation scripts
   - artifact checks
   - git diff status

5. Use explicit read-only subagents:
   - Baseline audit agent:
     Inspect current notebooks, scripts, utilities, artifacts, and repeated/generic content risk.
   - Library-use audit agent:
     Compare existing visualization choices against installed geometry stack and catalog.
   - One chapter planner per chapter, or per small chapter batch:
     Compare source pages, existing notebook, existing artifacts, missing explanations, weak visuals, stale paths, and library opportunities.
   - QC planner:
     Propose validation strategy and anti-generic checks.

For each chapter, produce an Improvement Brief:

1. Chapter id, title, source page span, and current notebook path.
2. Current quality summary.
3. Good existing work to preserve:
   - prose
   - helper code
   - visuals
   - artifacts
   - examples
   - checks
4. Missing standalone explanations.
5. Source-page concepts that are under-covered.
6. Existing visuals classified as:
   - strong and should keep
   - useful but needs labels/checks
   - decorative
   - generic/repeated
   - broken/stale
   - missing
7. Library-use diagnosis:
   - current libraries used
   - better installed libraries for this chapter
   - why the replacement or addition would improve teaching
8. New or improved visualization storyboard:
   For each planned visual:
   - concept
   - representation
   - chosen library
   - artifact path
   - learner inspection target
   - check/invariant
9. Worked examples or labs to add/refine.
10. Symbolic/numerical/mesh/topological checks to add/refine.
11. Artifact changes needed.
12. Utility/helper changes needed, if any.
13. Risk of breaking existing work.
14. Acceptance criteria.

Also produce:
- Baseline quality summary.
- Generic-content duplication summary.
- Library-opportunity summary.
- Proposed worker allocation:
  - one chapter worker per chapter
  - utilities worker
  - artifact/rendering worker only if needed
  - index worker after chapter edits
  - QC worker after implementation
- Validation sequence:
  - compileall
  - notebook audit
  - visual audit
  - anti-generic audit
  - artifact audit
  - limited nbclient validation
  - full validation only if needed
  - git diff --check
- Exact implementation prompt I should run next.