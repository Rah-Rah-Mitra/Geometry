I have created a new empty textbook folder inside D:\Geometry and placed the textbook PDF in it.

This turn is PLANNING ONLY.

Goal:
Turn this folder into a standalone visualization-first notebook course where every chapter is planned from its own source pages and taught through appropriate geometry visualizations, symbolic checks, computational experiments, and applied labs.

Use the repo-local skills in D:\Geometry\.codex\skills. Explicitly open and follow:
- geometry-visualization-planner
- geometry-chapter-notebook-author
- geometry-notebook-qc

Also explicitly read the repo-local geometry library catalog used by these skills before choosing libraries:
- .codex\skills\geometry-visualization-planner\references\geometry-library-catalog.md
- any matching catalog copy under the author or QC skill, if present

Important failure mode to avoid:
Do not plan a generic notebook factory. Do not plan repeated generic notebooks with the same cells, same visuals, same examples, or same artifact pattern. Every chapter must be planned after reading that chapter’s assigned source pages.

Planning-only rules:
- Do not modify files.
- Do not create notebooks, scripts, utilities, artifacts, or AGENTS.md yet.
- Do not write a bootstrap script in this turn.
- Inspect the PDF, repo structure, existing Geometry course patterns, existing AGENTS.md examples, repo-local skills, and installed library catalog.
- Infer title, parts, chapters, appendices, sections, page spans, and source-page boundaries.
- If PDF structure is ambiguous, inspect nearby pages and document assumptions.

Use explicit subagents:
1. Book-structure planner:
   - Read-only.
   - Infer title, chapter map, part hierarchy, appendices, and page spans.
2. Library-catalog planner:
   - Read-only.
   - Summarize installed geometry visualization stack and produce a library-routing guide for this textbook.
3. One visualization-planner subagent per chapter, or per small chapter batch if there are many chapters:
   - Read-only.
   - Must inspect assigned source pages.
   - Must produce a chapter-specific visualization storyboard.
4. QC planner:
   - Read-only.
   - Plan audits that detect generic notebooks, decorative visuals, stale artifacts, copyright problems, and weak standalone explanations.

For each chapter, produce a Chapter Implementation Brief with these fields:

1. Chapter id, title, source page span, and section/subsection map.
2. Core concepts:
   - definitions
   - constructions
   - theorem/proof targets
   - important examples
   - exercise themes
   - common learner misconceptions
3. Standalone teaching requirements:
   - what the notebook must explain so the PDF is not needed
   - what must be paraphrased
   - what should not be copied
4. Visualization-library routing:
   For each major concept, choose the best representation and library. Use the installed geometry stack where appropriate:
   - Matplotlib: durable 2D proof diagrams, constructions, incidence, angle, area, orientation, curves.
   - Plotly: interactive 2D/3D parameter exploration, surfaces, transformations, animated traces, HTML artifacts.
   - PyVista/VTK/Trimesh/MeshIO: 3D surfaces, meshes, normals, curvature, frames, triangulations, geodesics, polyhedra, slicing, manifold inspection.
   - gpytoolbox/potpourri3d/robust_laplacian/manifold3d/xatlas: mesh processing, Laplacians, geodesic computations, parameterizations, surface diagnostics.
   - SymPy: exact symbolic derivations, equations, identities, coordinate transformations, algebraic checks.
   - Galgebra/Clifford/Kingdon/PyGanja: geometric algebra, exterior algebra, rotors, bivectors, conformal/projective models, algebraic proof checks.
   - Gudhi/Ripser/Persim: filtrations, persistent homology, simplicial complexes, topology of point clouds, persistence diagrams.
   - Geomstats/PyRiemann: manifolds, metrics, geodesics, curvature intuition, SPD geometry, statistical geometry.
   - Shapely/Scipy.spatial/NetworkX: computational geometry, intersections, Voronoi/Delaunay, planar arrangements, graph/proof dependency diagrams.
   - OpenCV/Kornia/Torch/Scikit-image: projective geometry, transformations, epipolar geometry, image geometry, homographies, camera models.
   - Geopandas/Folium/PyDeck/OSMnx/Rasterio/PyProj: only for geographic geometry or map-based geometry.
   - POT/GeomLoss: optimal transport, Wasserstein geometry, barycenters, matching, metric geometry of distributions.
5. Visualization storyboard:
   For each visual, specify:
   - concept being taught
   - representation type
   - chosen library
   - why this library is appropriate
   - artifact filename under artifacts/
   - whether static, interactive, widget, animation, JSON check, or notebook-only
   - nearby prose explanation needed
   - learner inspection target
   - validation/check attached to the visual
6. Worked examples and computational labs.
7. Symbolic, numerical, mesh, topology, or geometric sanity checks.
8. Expected notebook outline with section headings.
9. Reusable utilities likely needed.
10. Artifact plan.
11. Copyright risks:
   - textbook prose not copied
   - no long exercise text
   - no screenshots
   - no page crops
   - no reproduced page layout
12. Implementation risks:
   - heavy computation
   - widget fragility
   - offscreen rendering issues
   - dependency uncertainty
   - ambiguous source pages
13. Acceptance criteria.

Also produce:
- Proposed course folder structure:
  - 00-book-index.ipynb
  - AGENTS.md
  - artifacts/
  - scripts/
  - utils/
  - chapter/part folders
- Proposed AGENTS.md outline.
- Proposed reusable utility modules.
- Proposed audit scripts:
  - index builder
  - notebook audit
  - visual audit
  - anti-generic audit
  - artifact-path audit
  - nbclient validation
- Proposed subagent implementation allocation:
  - one chapter worker per chapter
  - one utilities worker
  - one artifact/rendering worker only if needed
  - one index worker after chapters complete
  - one QC worker after implementation
- Validation plan:
  - uv sync only if dependencies changed
  - compileall
  - notebook audit
  - visual audit
  - anti-generic audit
  - limited nbclient execution
  - git diff --check

Notebook contract:
- One canonical teaching notebook per chapter folder plus a 00-index.ipynb.
- Each canonical notebook must be authored directly as a chapter-specific lesson.
- Scripts may support validation, indexing, and reproducible artifact generation.
- Scripts must not mass-generate generic teaching notebooks.
- Shared utilities are allowed, but teaching cells, examples, visuals, and artifacts must be chapter-specific.
- Do not impose a fixed visual count.
- Every major concept should receive a visual, computational experiment, symbolic check, worked example, or documented reason why not.

End with:
1. Book structure table.
2. Installed-library routing table for this book.
3. Chapter brief table.
4. Worker allocation table.
5. Validation plan.
6. Exact implementation prompt I should run next.