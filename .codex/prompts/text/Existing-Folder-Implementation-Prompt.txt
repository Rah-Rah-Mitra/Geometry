Proceed with the visualization-first improvement pass using the approved planning output from the previous turn.

Use the repo-local skills in D:\Geometry\.codex\skills. Explicitly open and follow:
- geometry-visualization-planner
- geometry-chapter-notebook-author
- geometry-notebook-qc

Also explicitly read and use:
- .codex\skills\geometry-visualization-planner\references\geometry-library-catalog.md
- any related repo-local library catalog or course-local helper catalog

Primary objective:
Improve the existing textbook notebook course so the notebooks stand alone from the textbook and teach through chapter-specific visualizations, examples, checks, and labs.

Preservation rule:
Do not throw away good existing work. Preserve useful existing prose, helpers, artifacts, structure, and working code unless there is a clear reason to replace or revise them.

Hard prohibitions:
- Do not overwrite the course with a monolithic generator.
- Do not mass-rewrite notebooks from a generic template.
- Do not create or use a script that populates every chapter notebook with the same generic cells.
- Do not copy textbook prose, long exercise text, screenshots, page crops, or page-layout reproductions.
- Do not add decorative visuals that are not tied to chapter concepts.

Required workflow:

1. Re-inspect:
   - folder structure
   - AGENTS.md
   - scripts/
   - utils/
   - artifacts/
   - existing notebooks
   - textbook PDF
   - approved Improvement Briefs
   - library catalog

2. Run enough existing audits/validation to understand baseline before editing.

3. Use explicit subagents:
   - One chapter worker per chapter where practical.
   - Each chapter worker must read assigned source page span before revising.
   - Each chapter worker must use its approved Improvement Brief.
   - Each chapter worker may write only:
     - its chapter notebook/folder
     - its chapter-specific artifacts subtree
   - Shared utilities/scripts are handled by a separate utilities worker.
   - Index regeneration happens only after chapter work is complete.
   - QC happens after implementation.

4. For each chapter, improve the canonical notebook directly.
   Keep one canonical teaching notebook per chapter folder plus a 00-index.ipynb.

5. Each improved notebook must include:
   - title
   - source map with page span and section names only
   - standalone learning goals
   - original explanations
   - paraphrased definitions and theorem targets
   - equations
   - worked examples
   - chapter-specific visualizations
   - inline artifact display
   - symbolic/numerical/mesh/topology/geometric sanity checks
   - applied lab or exploration
   - pitfalls or misconceptions
   - takeaways

6. Use the installed geometry stack appropriately.
   Do not default to generic matplotlib-only treatment when the chapter clearly needs richer tools:
   - PyVista/Trimesh/VTK for 3D surfaces, meshes, normals, curvature, frames, slicing, polyhedra.
   - gpytoolbox/potpourri3d/robust_laplacian/manifold3d/xatlas for mesh processing and surface diagnostics.
   - SymPy/Galgebra/Clifford/Kingdon/PyGanja for symbolic, exterior, Clifford, and geometric algebra checks.
   - Gudhi/Ripser/Persim for topology, filtrations, and persistence.
   - Geomstats/PyRiemann for manifolds, geodesics, metrics, SPD geometry, and statistical geometry.
   - Shapely/Scipy.spatial/NetworkX for planar computational geometry and proof/dependency graphs.
   - Plotly/ipywidgets/ipympl for interactive transformation and parameter exploration.
   - OpenCV/Kornia/Torch/Scikit-image for projective/image geometry.
   - POT/GeomLoss for Wasserstein and transport geometry.
   - GIS libraries only when geography clarifies the geometry.

7. Save new artifacts under the existing book-local artifacts/ subtree.
   - Use chapter-specific names.
   - Display inline.
   - Assert existence/nonzero size in checks.
   - Store JSON/CSV invariant summaries when useful.

8. Add or update helper functions only when they improve clarity or remove repeated technical code.
   Do not move chapter-specific teaching logic into opaque helpers.

9. Update indexes only after chapter work is complete.

10. Add or update audits so they catch:
    - broken artifact links
    - missing inline artifact display
    - repeated generic markdown blocks
    - repeated generic headings beyond allowed boilerplate
    - missing chapter-specific source map
    - missing or unrealized storyboard items
    - stale artifact references
    - PDF screenshots/page crops
    - visuals not explained in nearby prose
    - missed use of appropriate installed geometry libraries

Quality bar:
- Each improved chapter must be recognizably tied to its own source pages.
- Each major visual must clarify a specific concept from that chapter.
- Every major concept should have a visual, computational experiment, symbolic check, worked example, or documented reason why not.
- No fixed visual count.
- Existing useful work should survive unless it conflicts with the goals.

Validation:
- Run compileall.
- Run notebook audit.
- Run visual audit if present.
- Run anti-generic audit.
- Run artifact audit.
- Run limited nbclient validation.
- Run full validation if shared utilities or many notebooks changed.
- Run git diff --check.
- Fix high-confidence failures.
- Report residual failures honestly.

Final report:
- Changed files.
- Improved chapters.
- Preserved existing assets or prose worth noting.
- New/updated artifacts.
- Chapter-specific library choices used.
- Scripts/utilities changed.
- Validation commands and results.
- Remaining gaps.