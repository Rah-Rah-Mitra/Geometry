Proceed with implementation using the approved planning output from the previous turn.

Use the repo-local skills in D:\Geometry\.codex\skills. Explicitly open and follow:
- geometry-visualization-planner
- geometry-chapter-notebook-author
- geometry-notebook-qc

Also explicitly read and use:
- .codex\skills\geometry-visualization-planner\references\geometry-library-catalog.md
- any related repo-local library catalog or course-local helper catalog

Primary objective:
Create a standalone visualization-first notebook course from the textbook PDF. A reader should not need the PDF open.

Hard prohibitions:
- Do not create a monolithic notebook-generation script.
- Do not mass-generate teaching notebooks from one generic template.
- Do not fill chapters with the same generic markdown, same examples, same diagrams, or same artifact set.
- Do not use scripts as a substitute for chapter authorship.
- Do not copy textbook prose, long exercise text, screenshots, page crops, or page-layout reproductions.
- Do not add decorative visuals that are not tied to a chapter concept.

Required workflow:

1. Confirm book structure.
   - Re-inspect the PDF and approved plan.
   - Confirm title, chapters, page spans, parts, appendices, and section hierarchy.

2. Create course structure:
   - 00-book-index.ipynb
   - AGENTS.md
   - artifacts/
   - scripts/
   - utils/
   - chapter/part folders

3. Write book-specific AGENTS.md.
   Include:
   - notebook-first authorship rule
   - no generic notebook factory rule
   - visualization-first contract
   - copyright constraints
   - artifact path rules
   - installed-library routing table
   - subagent write boundaries
   - validation expectations

4. Create reusable utilities only where they reduce repeated technical code:
   - artifact path helpers
   - notebook display helpers
   - plotting helpers
   - 2D construction helpers
   - 3D mesh/surface helpers
   - symbolic-check helpers
   - widget helpers
   - validation helpers

   Utilities must not hide the chapter-specific pedagogy. Prefer transparent notebook code when the code itself teaches the geometry.

5. Use explicit subagents:
   - One worker per chapter where practical.
   - Each chapter worker must read assigned source pages before editing.
   - Each chapter worker must use its Chapter Implementation Brief.
   - Each chapter worker must implement the notebook directly, not through a generic generator.
   - Each chapter worker may write only:
     - its chapter folder
     - its chapter-specific artifacts subtree
   - Shared utility/script changes belong to a utilities worker.
   - Index generation happens only after chapter work is complete.
   - QC happens after implementation.

6. For each chapter, author one canonical teaching notebook directly in the chapter folder, plus a 00-index.ipynb.
   Each notebook must include:
   - chapter title
   - source map with page span and section names only
   - standalone learning goals
   - original prose
   - paraphrased definitions and theorem targets
   - equations
   - worked examples
   - realized visualization storyboard
   - inline artifacts with stable relative paths
   - symbolic/numerical/geometric checks
   - applied lab or exploration
   - pitfalls or common misconceptions
   - takeaways
   - related-chapter pointers

7. Use appropriate geometry libraries, not just generic matplotlib.
   For each chapter, choose libraries from the approved library routing:
   - Matplotlib for durable 2D proof diagrams and labeled constructions.
   - Plotly for interactive parameter sweeps, transformations, and 3D HTML.
   - PyVista/VTK/Trimesh/MeshIO for surfaces, meshes, normals, curvature, slicing, polyhedra, and spatial inspection.
   - gpytoolbox/potpourri3d/robust_laplacian/manifold3d/xatlas for mesh processing, Laplacians, parameterization, and geodesic/surface diagnostics.
   - SymPy for exact identities, coordinate algebra, polynomial equations, and sanity checks.
   - Galgebra/Clifford/Kingdon/PyGanja for geometric algebra, exterior algebra, rotors, bivectors, conformal/projective models.
   - Gudhi/Ripser/Persim for topology, filtrations, persistent homology, simplicial complexes, and persistence diagrams.
   - Geomstats/PyRiemann for manifolds, metrics, geodesics, curvature intuition, SPD geometry, and statistical geometry.
   - Shapely/Scipy.spatial/NetworkX for computational geometry, planar arrangements, Delaunay/Voronoi, graph structures, and proof dependency graphs.
   - OpenCV/Kornia/Torch/Scikit-image for projective geometry, homographies, epipolar geometry, cameras, and image transformations.
   - POT/GeomLoss for optimal transport and Wasserstein geometry.
   - GIS libraries only when geographic geometry clarifies the chapter.

8. Save generated artifacts under artifacts/.
   - Use chapter-specific artifact names.
   - Display artifacts inline.
   - Add final checks that assert artifact existence and nonzero size.
   - Record key computed invariants in JSON/CSV when useful.

9. Add scripts:
   - build index
   - audit notebooks
   - audit visuals
   - audit artifacts
   - anti-generic audit
   - nbclient validation

10. Anti-generic audit must detect:
    - repeated markdown blocks across chapters
    - repeated identical headings beyond allowed boilerplate
    - same visual/artifact names reused across unrelated chapters
    - missing chapter-specific source map
    - missing realized storyboard items
    - visuals not referenced in nearby prose
    - broken artifact paths
    - artifacts outside artifacts/
    - PDF screenshots or page crops
    - notebooks that use only generic matplotlib when richer installed geometry libraries are clearly appropriate

Quality bar:
- Every chapter must be recognizably about its own source pages.
- Every major visual must answer a chapter-specific conceptual question.
- Every major concept must have a visual, computation, symbolic check, worked example, or documented reason why not.
- Do not impose a fixed visual count.
- Prefer fewer strong visuals over many decorative ones.
- Use the installed geometry stack aggressively but appropriately.

Validation:
- Run uv sync only if dependencies changed.
- Run compileall.
- Run notebook audit.
- Run visual audit.
- Run anti-generic audit.
- Run artifact-path audit.
- Run limited nbclient validation.
- Run full validation if shared utilities or many notebooks changed.
- Run git diff --check.
- Fix high-confidence failures.
- Report residual failures honestly.

Final report:
- Changed files.
- Chapter notebooks created.
- Chapter-specific library choices used.
- Artifacts generated by chapter.
- Utilities/scripts added.
- Validation commands and results.
- Any notebooks not fully executed and why.
- Remaining gaps.