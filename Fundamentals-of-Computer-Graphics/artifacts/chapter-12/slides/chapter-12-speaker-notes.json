{
  "slides": [
    {
      "slide": 1,
      "notes": "Open by framing data structures as promises about questions, not as neutral containers. A renderer keeps many representations because each one makes a different query cheap: adjacency, transform composition, ray culling, visibility order, or memory locality. The chapter is a catalog of those query promises."
    },
    {
      "slide": 2,
      "notes": "Use the map as the lecture contract. The sequence starts with surface representation, moves through hierarchical object organization, then studies ray and visibility acceleration, and ends with memory layout. Keep returning to the same diagnostic question: what can this representation answer without scanning everything?"
    },
    {
      "slide": 3,
      "notes": "Separate draw storage from mesh topology early. The indexed mesh is excellent when the GPU needs positions and face indices, but it cannot answer neighborhood questions by itself. Ask students which operation each column supports quickly, then point out that compactness and editability are different design goals."
    },
    {
      "slide": 4,
      "notes": "Treat the small pyramid as an audit object rather than a pretty model. Closed edges should have two incident faces, oriented edges should appear with opposite directions across a shared side, and Euler characteristic should match the expected sphere-like topology. These checks catch invisible connectivity errors."
    },
    {
      "slide": 5,
      "notes": "Triangle strips and fans are best presented as bandwidth structures. They reduce repeated vertex references for a coherent run of triangles, while a parity rule keeps orientations consistent. They are not a general answer to mesh surgery, because local editing still needs explicit adjacency information."
    },
    {
      "slide": 6,
      "notes": "Compare the three connectivity choices by the walk they enable. Triangle-neighbor storage is lean for triangle-only meshes, winged-edge generalizes around edges, and half-edge makes directional local traversal explicit. The cost is pointer volume and update discipline whenever topology changes."
    },
    {
      "slide": 7,
      "notes": "The scene graph slide should feel like a matrix stack made visible. Local transforms stay attached to nodes, and a traversal multiplies along the current path. Changing the car node moves its wheels, while rotating a wheel should not rewrite the ferry, mast, or world transform."
    },
    {
      "slide": 8,
      "notes": "Connect instancing to the same ownership idea as scene graphs. A transformed object can keep one local shape, while each ray is mapped backward into that local coordinate system. This avoids duplicating geometry and explains why ray parameters must be treated carefully under nonuniform transforms."
    },
    {
      "slide": 9,
      "notes": "This taxonomy prevents a common confusion. A BVH groups objects and allows overlapping boxes, a grid partitions space and lets objects occupy multiple cells, and a BSP recursively cuts space with planes. All reduce candidate tests, but they distribute responsibility differently."
    },
    {
      "slide": 10,
      "notes": "Make the slab test concrete by reading intervals on the ray. Each axis gives an entering and leaving range, and the box is hit only when the ranges overlap. The two parallel-ray cases are the useful edge cases: inside the slab survives, outside the slab rejects immediately."
    },
    {
      "slide": 11,
      "notes": "A BVH traversal is a disciplined refusal to test primitives too early. The ray first asks whether a node box can be hit, descends only into surviving children, and tests leaves after interval pruning. The hierarchy is object-based, so sibling boxes may overlap without violating the structure."
    },
    {
      "slide": 12,
      "notes": "Explain the grid as a spatial calendar for the ray. The DDA walk enumerates cells in increasing ray order, and each cell exposes only the primitives registered there. It shines when occupancy is balanced, but large or uneven objects can inflate candidate lists across many cells."
    },
    {
      "slide": 13,
      "notes": "Axis-aligned BSP and kd-style splits are about recursive half-spaces. They give clean near and far ordering for rays, but primitives that straddle a plane must be duplicated or split. That tradeoff is why split selection matters more than the drawing of the tree suggests."
    },
    {
      "slide": 14,
      "notes": "For visibility BSP trees, emphasize that the viewer position changes traversal order, not the tree itself. The signed plane function classifies the eye, then the far side is drawn before the root polygon and the near side. This is a painter-style ordering argument."
    },
    {
      "slide": 15,
      "notes": "Triangle cutting is the robustness slide. A good split preserves orientation, avoids tiny numerical classifications near the plane, and conserves area when child pieces are added. The epsilon band is not decorative; it prevents a nearly-on-plane vertex from manufacturing unstable slivers."
    },
    {
      "slide": 16,
      "notes": "Use the address diagrams to move from geometry to memory. Row-major order keeps horizontal neighbors close but separates vertical neighbors by a row stride. Tiling makes short two-dimensional neighborhoods more local, while padding and tile size become measurable costs rather than implementation trivia."
    },
    {
      "slide": 17,
      "notes": "Close by turning the chapter into a review checklist. For every representation, name the fast query, the hidden cost, and the invariant that reveals an implementation error. That habit is more transferable than memorizing one mesh structure, one BVH builder, or one tiling formula."
    }
  ]
}
