{
  "slides": [
    {
      "slide": 1,
      "notes": "Open by naming the chapter's shift in viewpoint. Earlier rendering chapters could talk as if a drawing command directly produced pixels; this lecture treats hardware as a negotiated contract among host code, resident device memory, pipeline state, and shader programs. The goal is to make blank frames diagnosable."
    },
    {
      "slide": 2,
      "notes": "Use the roadmap as a dependency order rather than a table of contents. A draw call only makes sense after the application has created resources, described vertex interpretation, installed shaders, supplied transforms, and selected texture and framebuffer state. Each later topic answers one failure mode in that chain."
    },
    {
      "slide": 3,
      "notes": "Emphasize that the pipeline diagram is a contract diagram. The horizontal arrows show data moving toward the display, while the vertical feeders show state that must already be current. When a frame is wrong, the debugging question is which contract was broken before the draw reached rasterization."
    },
    {
      "slide": 4,
      "notes": "This slide prevents a common misconception: OpenGL-style APIs are not just function libraries, they are state machines. The draw command reads the current program, vertex array, buffers, uniforms, textures, and framebuffer tests. Changing one binding can affect a later draw even if the later line of code looks harmless."
    },
    {
      "slide": 5,
      "notes": "Separate storage from interpretation slowly. The buffer owns bytes on the device, but the vertex array records how those bytes become named shader attributes. The same byte stream can be useful or meaningless depending on stride, offset, scalar type, component count, and attribute location."
    },
    {
      "slide": 6,
      "notes": "Use the wrong-stride panel as a visual explanation of corrupted geometry. The shader may be perfectly correct while receiving positions read from colors or normals read from texture coordinates. The address formula is the invariant: base plus vertex index times stride plus attribute offset."
    },
    {
      "slide": 7,
      "notes": "Frame shaders as programmable islands inside a larger fixed pipeline. The vertex shader is responsible for per-vertex outputs such as clip position and varyings; rasterization then interpolates varyings; the fragment shader chooses output color or other framebuffer values for each candidate fragment."
    },
    {
      "slide": 8,
      "notes": "Connect transformation uniforms to the end of the old hidden matrix-stack era. The application now assembles model, view, and projection matrices explicitly, sends them as uniforms, and the vertex shader applies them. Ask students to track where coordinates change spaces and where clipping becomes meaningful."
    },
    {
      "slide": 9,
      "notes": "The important idea is that attributes are not just attached to vertices; after rasterization they become interpolated fragment inputs. Barycentric weights give a compact mental model: colors, UVs, normals, and other varyings should stay inside the convex combination dictated by the triangle."
    },
    {
      "slide": 10,
      "notes": "Make the hidden framebuffer attachments visible. Fragment shading proposes values, but depth, stencil, and blending state decide what is kept, rejected, labeled, or combined. This is why a color shader can look correct in isolation while the final frame is empty or unexpectedly overwritten."
    },
    {
      "slide": 11,
      "notes": "Textures are best taught as data plus sampling state. The image alone does not determine the result; coordinates, wrap modes, filter modes, mip level, texture unit, and sampler binding all participate. The same checker data can clamp, repeat, or blur depending on the active sampler."
    },
    {
      "slide": 12,
      "notes": "Relate meshes to the memory structures from earlier chapters. Hardware usually wants linear vertex and index buffers, while modeling tools often remember richer adjacency. The lecture distinction is practical: keep enough topology offline for editing, then stream compact indexed triangles for drawing."
    },
    {
      "slide": 13,
      "notes": "Instancing is the reuse slide. One resident mesh can be drawn many times by changing per-instance transforms, materials, or attributes. The saving is not only fewer bytes transferred; it also makes the scene conceptually cleaner because shared geometry and instance-specific state are separated."
    },
    {
      "slide": 14,
      "notes": "Treat performance as a set of pressure points rather than a single speed number. Host uploads, state changes, vertex work, fragment overdraw, texture traffic, and synchronization stalls each have different symptoms. A good optimization begins by identifying which pressure point is active."
    },
    {
      "slide": 15,
      "notes": "The diagnostic tree is meant to be used before rewriting code. Start with the earliest contract that can make the frame fail: context, program, vertex array, transform, texture, then depth or stencil state. Visual debug shaders are useful because they turn invisible state into inspected color."
    },
    {
      "slide": 16,
      "notes": "Close by turning the lecture into a checklist students can actually use at a workstation. The five invariants cover resource residency, interpretation state, coordinate spaces, hidden buffers, and reuse. They are deliberately small because a small checklist survives the stress of debugging a broken frame."
    }
  ]
}
