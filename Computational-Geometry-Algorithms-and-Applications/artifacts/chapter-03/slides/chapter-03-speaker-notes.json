{
  "slides": [
    {"slide": 1, "notes": "Open the lecture by naming the modeling bargain. We will not try to solve every visibility question directly. Instead, we turn the gallery boundary into a simple polygon, prove that diagonals can decompose it into triangles, and then use a coloring argument to choose a small guard set. The number to protect carefully is floor n over three."},
    {"slide": 2, "notes": "Make the definitions operational. A simple polygon has no crossing boundary edges, so it gives a clean inside and outside. Visibility is a segment test: if every point of the segment from the guard to the target remains in the polygon, the target is visible. This framing is deliberately geometric and will let triangulation replace a difficult global search."},
    {"slide": 3, "notes": "Separate the vocabulary carefully. A diagonal is not any chord of the polygon; its endpoints are vertices, they are not neighbors on the boundary, and the segment must remain in the polygon. A triangulation is then a saturated non-crossing set of such diagonals. Saturation matters because it turns a combinatorial object into a geometric certificate that every remaining cell is a triangle."},
    {"slide": 4, "notes": "Present this as the engine behind induction. At a leftmost vertex, the triangle formed with its neighbors points into the polygon. If the opposite side is clean, we are done. If it is not clean, the triangle contains polygon vertices that explain the obstruction. Choosing the farthest witness from the neighbor chord avoids crossings and gives a diagonal from the leftmost vertex."},
    {"slide": 5, "notes": "Use the diagram to explain why the formula is independent of the triangulation chosen. A diagonal divides the polygon into two smaller polygons, and the diagonal endpoints belong to both subproblems. That double counting is exactly the plus two in n one plus n two equals n plus two. The induction then forces every triangulation to have n minus two triangles."},
    {"slide": 6, "notes": "Now connect triangulation to guarding. Once the polygon is triangulated, every point lies in at least one triangle. If the vertices can be colored so that each triangle has all three colors, then choosing any one color class places a guard at a vertex of every triangle. Choosing the smallest class gives the numerical bound by the pigeonhole principle."},
    {"slide": 7, "notes": "The dual graph gives a clean proof view. It has a node for every triangle and an edge for every shared diagonal. It is connected because the polygon is one region, and a cycle would enclose a hole or force incompatible separation in a simple polygon triangulation. Leaves are ears, so induction can remove a leaf triangle, color the rest, and extend the third color to the removed vertex."},
    {"slide": 8, "notes": "State the theorem with the floor, not a ceiling. The upper bound comes from triangulate, three-color, and choose the smallest color class. The lower bound is a family argument: make a comb with separated pockets so one guard cannot see into two distant teeth. The construction shows that the theorem is not merely convenient; its dependence on n is tight."},
    {"slide": 9, "notes": "Emphasize the distinction between a proof and an efficient procedure. The leftmost-vertex argument proves that a diagonal exists, but a direct recursive implementation may repeatedly search through long boundary portions and split off tiny pieces. The chapter therefore changes strategy: impose monotonicity with a sweep, then exploit monotone structure with a stack triangulation."},
    {"slide": 10, "notes": "Define monotonicity using horizontal lines because the sweep runs from top to bottom. A vertex type is determined by the vertical relation to its two neighbors and whether the interior angle is convex or reflex. Split and merge vertices are the dangerous ones because they create disconnected horizontal intersections. Mention the tie rule so students see that equal-height vertices do not leave the event order ambiguous."},
    {"slide": 11, "notes": "Use this slide as a proof sketch rather than a formal case table. If monotonicity fails, some horizontal line sees two separated pieces of the polygon. Move the line until the number of intersection components changes; the responsible event must be a reflex vertex with both adjacent vertices on the same side, hence split or merge. Conversely, either such vertex visibly creates a disconnected slice just above or just below it."},
    {"slide": 12, "notes": "The MAKE_MONOTONE algorithm is a plane sweep with memory. The queue tells us which vertex is next. The status tree tells us which boundary edge lies immediately to the left of an event. The helper field is the compact state: it remembers a processed vertex that might need a diagonal when the sweep later discovers the matching event below it."},
    {"slide": 13, "notes": "Do not dwell on memorizing every branch. The main idea is that split and merge events are repaired by diagonals to stored helpers. Start and end events mostly maintain the active boundary edges, while regular vertices behave according to which chain they occupy. The DCEL matters because adding a diagonal is not just drawing a segment; it rewires half-edges and creates two face cycles for later triangulation."},
    {"slide": 14, "notes": "After partitioning, each piece has a much simpler geometry. Sort the vertices from top to bottom. The stack records an unresolved chain in the current funnel. The reason this can be linear is that each vertex is pushed once and popped at most once. The visibility test is local because a monotone polygon prevents hidden pockets from appearing behind the sweep order."},
    {"slide": 15, "notes": "Give students the mental branching rule. Opposite-chain vertices can see across the monotone polygon, so the algorithm fans diagonals to the stack and then starts a new stack. Same-chain vertices require a turn test: pop and add diagonals only while the popped vertex remains visible from the current vertex. Since each stack operation is charged once, the whole pass is linear."},
    {"slide": 16, "notes": "Close by assembling the pipeline. The sweep adds diagonals that remove split and merge vertices, producing monotone pieces. The stack algorithm triangulates those pieces without returning to a global search. The dual tree supports a three-coloring, and the smallest color class gives the guard set. The same decomposition viewpoint is the bridge to triangulating more general planar subdivisions."}
  ]
}
