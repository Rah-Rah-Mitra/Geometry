{
  "slides": [
    {"slide": 1, "notes": "Open with the GIS windowing motivation. The old rectangular window structures are not the right primitive once the affected region follows wind or terrain, so the chapter triangulates the region and studies one triangle at a time. Frame the lecture as a storage versus query-time story."},
    {"slide": 2, "notes": "Translate the polygon problem into the smallest non-orthogonal query. A point is in the triangle exactly when it passes three side tests. This matters because the partition tree algorithm will be described for a half-plane first, and the triangle version is the same recursion with three possible boundary crossings."},
    {"slide": 3, "notes": "Use the one-dimensional tree to introduce canonical subsets before the geometry gets heavy. Each subtree is a precomputed subset with a stored count. The query never enumerates all points if it can select a whole subtree, and the selected subtrees are disjoint by construction."},
    {"slide": 4, "notes": "Define the object that replaces binary splits in the plane. The triangles are covers for classes, not cells of a planar subdivision, so they may overlap. The key accounting property is that each point belongs to exactly one class, while the fine condition prevents one child from carrying too much of the input."},
    {"slide": 5, "notes": "Now isolate the parameter that drives query time. If the boundary line misses a triangle, that whole class is either inside or outside the half-plane. Only crossed triangles need recursion, so Matousek's low-crossing partition theorem is the hinge between geometry and the later recurrence."},
    {"slide": 6, "notes": "Connect the partition to a data structure. Every child is both a geometric triangle and an accounting record for a subset of points. Recursion is only on the class, so the tree stores many canonical subsets that can later be selected wholesale by a query."},
    {"slide": 7, "notes": "Walk through the query at one node. The important point is that a selected child contributes its stored count immediately; it is not searched further. Rejected children contribute nothing, and only boundary-crossed children keep the recursion alive."},
    {"slide": 8, "notes": "Make the triangle extension feel almost anticlimactic. The recursion has the same three cases as the half-plane query. The only analysis change is that the boundary has three lines, and a class can be ambiguous only when one of those lines crosses its enclosing triangle."},
    {"slide": 9, "notes": "Summarize the proof dependencies rather than redoing every algebraic detail. Disjointness and fine balance are what make the storage linear. Low crossing number limits the branching of the query recursion, and a sufficiently large constant r absorbs constants into the epsilon."},
    {"slide": 10, "notes": "Use segment-line intersection to show why multi-level structures appear before duality and cuttings. The condition has two endpoint predicates, so a first-level search selects canonical subsets for one endpoint and associated trees filter them by the second endpoint. This is the same canonical-subset philosophy, nested."},
    {"slide": 11, "notes": "Pause to explain why a second structure is needed. Partition trees are good because storage is linear, but the query time is not logarithmic. Full arrangements suggest that logarithmic time is possible if enough information is precomputed. Cuttings are the structured way to make that idea recursive."},
    {"slide": 12, "notes": "Introduce duality as a problem transformation, not as a decorative symmetry. Under the chosen convention, above in the primal becomes below in the dual. The data structure will now partition the dual plane, while the original points have become lines."},
    {"slide": 13, "notes": "State the most common source of confusion directly. A cutting is a triangulation of the dual plane, and the objects being controlled are lines crossing cells. The cells are disjoint, but a line may be stored in several crossing subsets, which is why storage grows quickly."},
    {"slide": 14, "notes": "Contrast this with partition-tree queries. A partition tree may branch into many crossed children, while a cutting-tree point query descends through one containing cell. The cost is that each cell stores information about lower, upper, and crossing line subsets, and the total storage is near quadratic."},
    {"slide": 15, "notes": "Assemble the final data structure. The triangle is not stored as a primal object in the cutting tree; after duality it is three side tests on lines. Each level handles one test, and associated cutting trees carry the selected canonical subsets forward without breaking the disjointness needed for counting."},
    {"slide": 16, "notes": "Close by giving students the durable mental model. Partition trees are the linear-space end of the spectrum, cutting trees are the logarithmic-query end, and hybrids interpolate by switching structures when storage budget becomes tight. The checks listed on the slide are the implementation version of the proof obligations."}
  ]
}
