{
  "section_id": "section-13-support-functions",
  "artifact": "support-sum-ledger",
  "description": "Slide-local operation ledger for support functions. Values are original teaching checks, not copied source data.",
  "operations": [
    {
      "set_operation": "Minkowski sum",
      "support_identity": "h_{A+B}(u) = h_A(u) + h_B(u)",
      "condition": "A and B non-empty; extended real arithmetic allowed",
      "teaching_use": "Linear probe can optimize over each summand independently."
    },
    {
      "set_operation": "Convex hull of union",
      "support_identity": "h_{conv(A union B)}(u) = max(h_A(u), h_B(u))",
      "condition": "Non-empty sets; closure does not change support values",
      "teaching_use": "A linear probe lands on whichever set has the larger exposed value."
    },
    {
      "set_operation": "Scalar multiple",
      "support_identity": "h_{lambda A}(u) = lambda h_A(u) for lambda >= 0",
      "condition": "For negative lambda, probe direction changes sign",
      "teaching_use": "Positive homogeneity is the visible fingerprint of support functions."
    },
    {
      "set_operation": "Closed convex reconstruction",
      "support_identity": "C = intersection_u {x : x.u <= h_C(u)}",
      "condition": "C is closed and convex",
      "teaching_use": "All supporting halfspaces recover the set."
    }
  ]
}
