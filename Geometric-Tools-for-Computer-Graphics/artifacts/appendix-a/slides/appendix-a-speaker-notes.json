{"slides":["Open by naming the appendix as infrastructure rather than a side topic. Graphics algorithms often look geometric on the screen, but their correctness depends on pivot choices, residual checks, and representation decisions. The printed span is large, so this lecture will organize it as a sequence of contracts a geometry program must keep.","Use this map to connect the notebook route to the appendix. The important pattern is model, draw, perturb, measure, and check. Ask students to identify which quantities are geometric invariants and which are bookkeeping choices introduced by arrays, parameter domains, or finite precision arithmetic.","This is the first concrete numerical contract. A linear solve is not done when a vector of unknowns appears; it is done when the algorithm can report a small residual, a defensible pivot history, and a warning when conditioning makes the displayed geometry unreliable.","Polynomial systems are shown as an elimination pipeline because graphics intersections often hide several variables. Emphasize that eliminating a variable can raise degree and introduce candidate roots that still need substitution checks. The solver therefore returns a set of candidates plus residual tests, not just roots.","Treat decompositions as geometric lenses. QR separates orthogonal directions from triangular coefficients, SVD exposes principal stretching and rank, and polar decomposition factors a general linear map into rotation plus symmetric stretch. The same matrix can be understood differently depending on the diagnostic question.","This slide should prevent a common implementation mistake: rotation representations are not interchangeable storage formats. Matrices compose directly but drift from orthogonality, axis-angle is compact but has branch choices, and quaternions are efficient for interpolation when they are kept on the unit sphere.","Root finding is about controlling a search, not hoping an iteration lands correctly. Brackets give a certificate that a sign change has been isolated, Newton steps give speed when derivatives behave, and the stable quadratic formula shows why algebraically equivalent expressions can differ numerically.","Present minimization as root finding with geometry attached. The contour picture gives students a place to see line search, direction choice, and stopping rules. For quadratic forms, the eigenstructure determines curvature, so the algorithm should report both the candidate point and the residual in the gradient or constraints.","Least squares fitting should be taught as a geometric projection problem. The model parameters are useful, but the residual arrows are the audit trail. A renderer or modeler needs to know whether the point cloud is close to the fitted primitive or whether the fit is hiding anisotropic error.","Subdivision turns a continuous curve into a finite drawing, so the stopping rule is part of the geometry. Uniform samples are easy, arc length samples are perceptually steadier, midpoint distance catches chord error, and variation checks ask whether tangent direction has stabilized enough for the task.","Use the calculus slide to reconnect numerical methods with the earlier geometry chapters. Level sets are implicit primitives, extrema mark optimization targets, and Lagrange multipliers explain constrained contacts by aligning gradients. The visual target is the relationship between a constraint curve and the objective level set.","This warning slide is about professional practice. Many bugs survive because the picture looks plausible while the residual is large or the coordinate convention changed. Ask students to classify each failure by the violated contract: conditioning, representation, stopping rule, or an invariant that was never checked.","The lab slide turns the lecture into a reproducible workflow. Students should choose one primitive, vary one tolerance, and record one residual. The point is to make a small computation accountable: it must say what was modeled, what was perturbed, and why the resulting number supports the picture.","Close by making the appendix feel central to the course. Numerical methods are the tools that keep geometric statements alive inside finite precision code. A strong implementation reports the answer, the representation choices, the residual, and the failure mode that would make the answer untrustworthy."]}
